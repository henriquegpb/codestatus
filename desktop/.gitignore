# Never committed: ~370 MB, and the Electron binary is specific to platform and
# architecture, so a copy from one machine is useless on another.
node_modules/

# Generated for eyeballing the artwork: `npm run icons` and `npm run preview -- --shots`.
test/icons/
test/shots/
